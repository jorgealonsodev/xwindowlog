//! `SessionMonitor` trait (D-13), `ZbusSessionMonitor`, `FakeSessionMonitor`, the `xwl-logind`
//! bridge thread and its `eventfd` (D-3). The only file naming a `zbus` type.

#[cfg(test)]
mod tests {}
